type Query {
    deviceById(id: ID): Device
}

type Device {
    id: ID
    name: String
    type: DeviceType
    status: String
    state: String
}

type DeviceType {
    id: ID
    name: String
    icon: String
}